# =============================================================================
# CONFIGURA��O DO AMBIENTE � backend emerson-se
# =============================================================================
# Copia este ficheiro para .env e preenche os valores.
# NUNCA cometer .env com credenciais reais para o reposit�rio.
# =============================================================================

# --- Servidor ---
PORT=3000
FRONTEND_URL=https://emerson-se.web.app

# --- Firebase (Firestore) ---
# JSON completo do service account, numa �nica linha (sem quebras de linha
# reais nos private_key � usa \n para as quebras dentro da string JSON).
#FIREBASE_SERVICE_ACCOUNT=""

# --- Email (Gmail SMTP) ---
# App password gerada em https://myaccount.google.com/apppasswords
# N�o � a password normal da conta Gmail!
EMAIL_USER=""
EMAIL_APP_PASSWORD="
EMAIL_DESTINO=""
FIREBASE_SERVICE_ACCOUNT=""
